This looks like a brand-new repository with no domain glossary yet (no `CONTEXT.md`). Take a moment to plant the project's shared vocabulary early, before it drifts.

1. Invoke the `domain-modeling` skill (via the Skill tool).
2. As you and the user name the core concepts of this project, capture each resolved term in a root `CONTEXT.md`, created lazily once the first term is worth writing down. Keep `CONTEXT.md` a glossary and nothing else: no implementation details, no spec, no scratch notes.
3. This is a lightweight nudge to establish vocabulary, not a design interview. Do not start a grilling. Only offer an ADR for a decision that is hard to reverse, surprising without context, and the result of a real trade-off.

If there is not yet enough of a domain to model (an essentially empty repo), ignore this and carry on; the vocabulary will be worth capturing once real concepts appear.
